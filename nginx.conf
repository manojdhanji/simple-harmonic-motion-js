server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    add_header Cache-Control "no-store" always;

    location / {
        try_files $uri $uri/ /shm.html;
    }
}
